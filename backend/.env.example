FIREBASE_SERVICE_ACCOUNT_KEY=serviceAccountKey.json
GOOGLE_EARTH_ENGINE_PROJECT=<your-gee-project-id>

# For server deployments — paste the JSON content of your GEE service account key
# Leave empty to use local `gcloud auth application-default login` instead
GEE_SERVICE_ACCOUNT_KEY=
